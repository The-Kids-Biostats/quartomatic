# Launch Template Logo Editor (Shiny) — package-local addin

Replace the logo inside inst/ext_qmd/\_extensions/html or create a
modified zip. Writes logo_meta.json with timestamp. After save, opens
destination folder in OS file explorer or RStudio Files pane so user can
navigate to the saved logo.

## Usage

``` r
template_logo_edit()
```

## Value

A Shiny application that runs within RStudio.
